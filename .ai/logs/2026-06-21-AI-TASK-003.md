# AI-TASK-003 작업 로그

## 프롬프트 요약

홈 위치 변경 지도에서 서울 행정동별 경계와 이름 마커를 표시하고, 선택한 행정동 전체 영역을 색으로 강조한다. 쪽지 지도는 기존 정밀 위치 선택 방식을 유지한다.

## 조사 및 데이터

- 카카오 지도 `Polygon`, `CustomOverlay`, `coord2RegionCode` 동작 확인
- `vuski/admdongkor`의 `ver20260401/HangJeongDong_ver20260401.geojson` 사용
- 전국 원본 33MB에서 서울특별시 행정동 427개만 추출
- 좌표를 소수점 5자리로 정규화해 460KB 정적 GeoJSON 생성
- 기준일: 2026-04-01
- 좌표계: WGS84 (EPSG:4326)

## 확인한 파일

- `src/pages/NoteLocationPage.tsx`
- `src/features/map/types.ts`
- `src/features/map/lib/kakaoMap.ts`
- `src/features/home/types/homeTypes.ts`
- `package.json`

## 변경한 파일

- `.ai/tasks/AI-TASK-003.md`
- `.ai/logs/2026-06-21-AI-TASK-003.md`
- `public/data/seoul-administrative-dongs.geojson`
- `src/features/map/lib/seoulAdministrativeDongs.ts`
- `src/features/map/types.ts`
- `src/pages/NoteLocationPage.tsx`

## 구현 결과

- 홈 위치 모드에서 서울 행정동 경계와 동 이름 라벨 표시
- 선택한 동은 주황색 반투명 면, 굵은 경계선, 주황색 라벨로 강조
- 경계 또는 라벨 클릭 시 해당 행정동 중심으로 이동하고 선택 상태 갱신
- 홈 복귀 시 선택한 행정동 이름 저장
- 쪽지 위치 모드에는 경계 데이터를 로드하거나 표시하지 않음
- 경계 파일은 지도 진입 시 별도 요청하고 메모리에 캐시

## 실행한 명령

- 최신 원본 GeoJSON 다운로드
- `jq`로 서울 행정동 필터링 및 좌표 정규화
- `npm run lint`
- `npm run build`
- GeoJSON 기준일·개수·서울 코드 검증
- `git diff --check`

## 검증 결과

- ESLint 통과
- TypeScript 및 Vite 프로덕션 빌드 통과
- GeoJSON: 기준일 2026-04-01, 서울 행정동 427개 확인
- 홈 지도: `휘경2동` 선택 면 표시 확인
- 인접 라벨 클릭: `휘경1동`으로 강조 영역과 하단 정보 전환 확인
- 홈 복귀: `휘경1동` 표시 확인
- 홈 검색: 서울 결과만 허용하며 `부산 해운대` 검색 시 지도 이동 없이 제한 안내 표시 확인
- 홈 지도 중심을 직접 서울 밖으로 이동한 경우에도 중심 좌표를 거부하고 설정 완료를 차단
- 쪽지 검색도 서울 결과만 허용하고, 제한 안내는 상단 바 아래 절대 배치해 뒤로가기·검색창 정렬을 유지
- 쪽지 최근 위치 UI와 로컬 저장·복원 로직을 제거해 두 위치 설정 패널의 높이를 통일
- 쪽지 지도: 50m 축척, 정밀 핀·도로명 주소 유지, 행정동 라벨 0개 확인
- 브라우저 콘솔 오류 없음
- 기존 500KB 초과 번들 경고는 남아 있으며 오류는 아님

## 리뷰 결과

- 시각 판정: 통과(94/100)
- 선택 영역과 비선택 영역의 위계가 명확하고, 모바일 지도에서 동 경계와 동 이름을 함께 파악할 수 있음
- 데이터 기준일 이후 행정동 개편 시 정적 GeoJSON 교체 필요
